import { SigctSeparateurDecimalPipe } from './sigct-separateur-decimal.pipe';

describe('SigctSeparateurDecimalPipe', () => {
  it('create an instance', () => {
    const pipe = new SigctSeparateurDecimalPipe();
    expect(pipe).toBeTruthy();
  });
});
