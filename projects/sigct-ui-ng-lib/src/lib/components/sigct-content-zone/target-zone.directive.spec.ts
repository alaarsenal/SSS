import { TargetZoneDirective } from './target-zone.directive';

describe('TargetZoneDirective', () => {
  it('should create an instance', () => {
    const directive = new TargetZoneDirective();
    expect(directive).toBeTruthy();
  });
});
