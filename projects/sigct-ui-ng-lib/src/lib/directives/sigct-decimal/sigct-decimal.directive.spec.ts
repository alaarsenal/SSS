import { SigctDecimalDirective } from './sigct-decimal.directive';

describe('SigctDecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new SigctDecimalDirective();
    expect(directive).toBeTruthy();
  });
});
