import { SigctOnlyNumberDirective } from './sigct-only-number.directive';

describe('SigctOnlyNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new SigctOnlyNumberDirective();
    expect(directive).toBeTruthy();
  });
});
