import { SigctDateDirective } from './sigct-date.directive';

describe('SigctDateDirective', () => {
  it('should create an instance', () => {
    const directive = new SigctDateDirective();
    expect(directive).toBeTruthy();
  });
});
