import { SigctTelDirective } from './sigct-tel.directive';

describe('SigctTelDirective', () => {
  it('should create an instance', () => {
    const directive = new SigctTelDirective();
    expect(directive).toBeTruthy();
  });
});
