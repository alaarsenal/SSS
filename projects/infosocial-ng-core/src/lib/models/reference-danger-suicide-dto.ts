import { ReferenceDTO } from './reference-dto';

/**
 * Classe représentant une donnée d'une table de référence. 
 * Elle est une repésentation js de la classe ca.qc.gouv.msss.sigct.infosocial.model.support.ReferenceDangerSuicideDTO
 */
export class ReferenceDangerSuicideDTO extends ReferenceDTO {
    codeCouleur : string;
}